import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "CarSite - Premium Car Dealership",
  description: "Find your dream car at CarSite - Premium cars, competitive prices, and excellent service",
  keywords: "cars, dealership, vehicle, buy car, car sale, Toyota, BMW, Mercedes",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
